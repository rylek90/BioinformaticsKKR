<Page x:Class="BioinformaticsKKR.View.AssembleView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="AssembleView">

    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <ComboBox ItemsSource="{Binding SequencesAssemblers}" 
                  SelectedItem="{Binding CurrentAssembler, Mode=TwoWay}"
                  Grid.ColumnSpan="2"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Status: " />
        <Label Grid.Row="2" Grid.Column="1" Content="{Binding LastStatus, Mode=TwoWay}" />
    </Grid>
</Page>
